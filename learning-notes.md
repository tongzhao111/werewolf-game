# 从看不懂代码到做出 AI 狼人杀：一个转行者的 AgentScope 学习记录

> 一个非科班、只会 `def` 和字符串的转行者，如何借助 AI 完成人生第一个多智能体项目。

---

## 一、我做了什么

一个月前，我还只是个"会一点 Python 但看不懂代码"的转行者。

现在，我 GitHub 上有了一个能跑的项目：

**🐺 三国狼人杀 · 多智能体 AI 对战**
🔗 https://github.com/tongzhao111/werewolf-game

8 个 AI 智能体分别扮演刘备、曹操、孙权、诸葛亮、司马懿、周瑜、张飞、关羽，自动完成一整局狼人杀——狼人协商击杀、预言家查验、女巫用药、守卫守护、白天辩论、投票淘汰、遗言反转，**全程无需人工干预**。

**跑 20 局的数据**：

- 好人胜率：40%
- 狼人胜率：60%
- 平均回合数：3.2

> 顺便说一句：40% vs 60% 这个比例，和真人线下狼人杀的平衡点几乎一致（真人 9 人局狼人胜率约 58%）。

**还能自己下场玩**：打开 Streamlit 网页，你能扮演村民，跟 7 个 AI 一起玩。

---

## 二、为什么选 AgentScope

我做这个项目之前对比了几个框架：

| 框架 | 特点 | 适合谁 |
|------|------|--------|
| AutoGen | 对话驱动，适合快速原型 | 有经验的开发者 |
| LangGraph | LangChain 生态，流程图式编排 | 熟悉 LangChain 的人 |
| **AgentScope** | **工程化优先，消息驱动，生产级** | **想学"正规军"打法的人** |

我最终选了 **AgentScope 2.0**，原因：

1. **阿里巴巴达摩院出品**——中文文档齐全，遇到问题好搜
2. **消息驱动架构**——天然适合"多个 AI 互相聊天"这种场景
3. **结构化输出**——能用代码强约束 AI 的输出格式，狼人杀里投票、查验这类决策特别需要

现在回头看，这个选择是对的。

---

## 三、学习路径：我走过的 5 个阶段

### 阶段 1：环境搭建（几天完成，然后搁置了一年）

说个坦白话：环境搭建其实**只花了几天**。

Python 3.12.10、Anaconda、PyCharm、AgentScope、DeepSeek API Key——每一步都不算难，跟着教程走下来，几天就搭好了。

**然后，我把它放在那里，整整一年没再打开。**

为什么？因为搭完环境之后，我卡在了第二步——**不知道下一步该做什么**。教程里的代码复制进去能跑，但稍微改一下我就懵。看着屏幕上那个 `main.py`，我心里只有一个念头："这玩意儿到底能干嘛？"

那一年里，我偶尔想起这个项目，也会想"我是不是不适合学 AI"。

**直到最近，我才重新打开 PyCharm，这一次——完成了。**

从重新打开到 push 到 GitHub，实际只用了 2 天。

所以真正难的从来不是环境，是**迈出下一步的决心**。

### 阶段 2：第一个 Agent（Hello World）

第一个能跑的代码，是让一个 AI 自我介绍：

```python
from agentscope.agent import Agent
from agentscope.model import DeepSeekChatModel

agent = Agent(name="Assistant", ...)
reply = await agent.reply(UserMsg(name="User", content="你好"))
print(reply.content[0].text)